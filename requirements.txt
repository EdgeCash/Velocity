# Streamlit Community Cloud installs from this file to run app/streamlit_app.py.
# The app is deliberately standalone (reads the runner's persisted parquets);
# it does not need the velocity package itself. Dev/CI installs use pyproject.
streamlit>=1.36
pandas>=2.1
pyarrow>=15.0
requests>=2.31
