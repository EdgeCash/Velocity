-- The accuracy chain: one row per graded game — the model's pregame numbers,
-- the final, and where the final sat on the pregame distribution.
select * from 'sources/velocity/data/accuracy.parquet'
